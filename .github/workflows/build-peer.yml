name: build peer binaries

on:
  push:
    paths: ["peer/**"]
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            name: nab-peer-windows.exe
            script: peer/nab_peer.py
          - os: macos-latest
            name: nab-peer-macos
            script: peer/nab_peer.py
          - os: ubuntu-latest
            name: nab-peer-linux
            script: peer/nab_peer.py
          - os: windows-latest
            name: nab-generator-windows.exe
            script: peer/nab_factory.py
          - os: macos-latest
            name: nab-generator-macos
            script: peer/nab_factory.py
          - os: ubuntu-latest
            name: nab-generator-linux
            script: peer/nab_factory.py
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pyinstaller
      - run: pyinstaller --onefile --name nab ${{ matrix.script }}
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: dist/nab${{ runner.os == 'Windows' && '.exe' || '' }}
